import SiteFooter, { type SiteFooterBlok } from '@/components/sections/SiteFooter/SiteFooter'

export default function SiteFooterBlok({ blok }: { blok: SiteFooterBlok }) {
  return <SiteFooter blok={blok} />
}
